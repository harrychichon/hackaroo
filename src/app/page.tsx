import IconButton from '@/components/ui/IconButtons/IconButton';
import styles from './page.module.css';
import CardFront from '@/components/ui/card/CardFront/CardFront';

export default function Home() {
	return <div className={styles.page}>Hello</div>;
}
 <IconButton onClick={() => {}} width={24} height={24} type="Own" />

 <CardFront image={require('@/path/to/image.jpg')} />